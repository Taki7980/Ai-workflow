---
description: Make one safe small project change with one focused check
---

Small project task. Read `AGENTS.md` and `.ai/HANDOFF.md`; run `brain-recall.ps1` once with task keywords. Confirm small-lane criteria, inspect only named code, make the minimum change, run one focused check, then replace HANDOFF and run `complete-task.ps1` once. Pass capture fields only for a verified reusable lesson; pass incident fields only for an explicitly named open incident. Escalate to full lane if scope or risk grows.
